import React from "react";

function Form(props) {

    return (
        <div className="field">
            <div className="control">
                <input className="input" type="text" placeholder="Add another concern"/>
            </div>
        </div>
    );
}

export default Form;